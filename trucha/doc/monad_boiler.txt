
import FailState(ErrMsg, FailState, evalFS)

func :: Args -> Either ErrMsg Result
func args = evalFS initialState (funcM args)
  where initialState = FuncState

data FuncState = FuncState
type M = FailState FuncState

funcM :: Args -> M Result

